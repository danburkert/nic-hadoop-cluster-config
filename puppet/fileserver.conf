# /etc/puppet/fileserver.conf

# Mount the /opt/rpm folder to rpm
# This folder should hold rpms to be installed on the puppet clients
[private]
  path /opt/private
  allow *.nearinfinity.com

[repo]
  path /home/localadmin/nic-hadoop-cluster-config/
  allow *.nearinfinity.com
